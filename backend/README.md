# Backend Workspace

`backend/` 目录当前包含 3 个后端相关交付单元：

- `api-server/`
  FastAPI 主服务，提供后台管理接口、前台数据接口和组学能力接口。
- `sdk/`
  面向外部程序调用的 Python SDK。
- `mcp/`
  面向智能体与工具生态的 MCP 服务。

推荐阅读顺序：

1. `api-server/README.md`
2. `../docs/后台后端设计思路.md`
